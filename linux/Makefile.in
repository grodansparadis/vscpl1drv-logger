# =========================================================================
#                      Autogenerated from Makefile.in
# =========================================================================

# STATIC = yes if a static build is requested.
STATIC=@STATIC@
VSCP_PROJ_BASE_DIR=@VSCPDIR@

prefix = @prefix@
exec_prefix = @exec_prefix@
INSTALL = @INSTALL@
top_srcdir = @top_srcdir@
top_builddir = @top_builddir@

CC = @CC@
CXX = @CXX@

# get version from the version file
MAJOR_VERSION = `sed '35!d' ${VSCP_PATH}/src/vscp/common/version.h  | cut -b 33-`
MINOR_VERSION = `sed '36!d' ${VSCP_PATH}/src/vscp/common/version.h  | cut -b 33-`
RELEASE_VERSION = `sed '37!d' ${VSCP_PATH}/src/vscp/common/version.h  | cut -b 33-`
BUILD_VERSION = `sed '38!d' ${VSCP_PATH}/src/vscp/common/version.h  | cut -b 33-

CFLAGS = @CFLAGS@ -fPIC -DCBC -I${VSCP_PATH}/src/common -I${VSCP_PATH}/src/vscp/common
CXXFLAGS = -D__LINUX__ -fPIC -D_REENTRANT @CXXFLAGS@
CPPFLAGS = -D__LINUX__ -fPIC @CPPFLAGS@ -fno-var-tracking-assignments -I${VSCP_PATH}/src/common -I${VSCP_PATH}/src/vscp/common

LDFLAGS = @LDFLAGS@
EXTRALIBS = @EXTRALIBS@

DLFLAGS = -g -shared -fPIC
STRIP = strip
INSTALL_PROGRAM = ${INSTALL}
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_DIR = /usr/bin/install -c -d
srcdir = .
top_srcdir = .
top_builddir =
bindir = ${exec_prefix}/bin
libdir = ${exec_prefix}/lib
datadir = ${prefix}/share
includedir = ${prefix}/include
DLLPREFIX = lib


### Variables: ###


CANALLOGGER_OBJECTS =  vscpl1drv-logger.o\
	log.o\
	dllist.o

### Targets: ###

all: vscpl1drv-logger.so


test:  test.o $(PROJOBJS)
	$(CXX) -o $@ test.o $(PROJOBJS) $(LDFLAGS) -L$(LIBDIRNAME) $(EXTRALIBS)

vscpl1drv-logger.so: $(CANALLOGGER_OBJECTS)
	$(CXX) -o $@ $(CANALLOGGER_OBJECTS) $(DLFLAGS) -L$(LIBDIRNAME) $(EXTRALIBS)

log.o: ../common/log.cpp ../common/log.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c ../common/log.cpp -o $@

guid.o: ${VSCP_PATH}/src/vscp/common/guid.cpp ${VSCP_PATH}/src/vscp/common/guid.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c ${VSCP_PATH}/src/vscp/common/guid.cpp -o $@

dllist.o: ${VSCP_PATH}/src/common/dllist.c ${VSCP_PATH}/src/common/dllist.h
	$(CC) $(CFLAGS) -c ${VSCP_PATH}/src/common/dllist.c -o $@

install: all
	mkdir -p $(VSCP_PROJ_BASE_DIR)/drivers/level1
	$(INSTALL_PROGRAM) vscpl1drv-logger.so $(VSCP_PROJ_BASE_DIR)/drivers/level1

uninstall:

install-strip: install

deb:    all
	echo "\n" | ./builddist.sh
	@mkdir -p ../../../../../../dist
	@mv *.deb ../../../../../../dist

clean:
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../common/*.o
	rm -f ${VSCP_PATH}/src/common/*.o
	rm -f ${VSCP_PATH}/src/vscp/common/*.o
	rm -f vscpl1drv-logger.so
	rm -f test
	rm -f *.gz
	rm -f *.deb

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh Makefile

$(ALWAYS_BUILD):  .FORCE

.FORCE:

# Include dependency info, if present:
-include .deps/*.d

.PHONY: all install uninstall clean distclean data .FORCE
