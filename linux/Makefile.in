# =========================================================================
#                      Autogenerated from Makefile.in
# =========================================================================

# Package version
MAJOR_VERSION=@MAJOR_VERSION@
MINOR_VERSION=@MINOR_VERSION@
RELEASE_VERSION=@RELEASE_VERSION@
BUILD_VERSION=@BUILD_VERSION@
PACKAGE_VERSION=@PACKAGE_VERSION@

# STATIC = yes if a static build is requested.
STATIC=@STATIC@
VSCP_PROJ_BASE_DIR=@DESTDIR@

prefix = @prefix@
exec_prefix = @exec_prefix@
INSTALL = @INSTALL@
top_srcdir = @top_srcdir@
top_builddir = @top_builddir@

CC = @CC@
CXX = @CXX@

CFLAGS = @CFLAGS@ -fPIC -DCBC -I../_vscp_common_general -I../_vscp_common
CXXFLAGS = -D__LINUX__ -fPIC -D_REENTRANT @CXXFLAGS@
CPPFLAGS = -D__LINUX__ -fPIC @CPPFLAGS@ -fno-var-tracking-assignments -I../_vscp_common_general -I../_vscp_common

LDFLAGS = @LDFLAGS@
LIBS = @LIBS@
EXTRALIBS = @EXTRALIBS@

DLFLAGS = -g -shared -fPIC
STRIP = strip
INSTALL_PROGRAM = ${INSTALL}
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_DIR = /usr/bin/install -c -d
srcdir = .
top_srcdir = .
top_builddir =
bindir = ${exec_prefix}/bin
libdir = ${exec_prefix}/lib
datadir = ${prefix}/share
includedir = ${prefix}/include
DLLPREFIX = lib
LIB_SONAME = "vscpl1drv-logger"

### Variables: ###


CANALLOGGER_OBJECTS =  vscpl1drv-logger.o\
	log.o\
	dllist.o

### Targets: ###

all: vscpl1drv-logger.so test lib

# static lib
lib:	vscpl1drv-logger.so
	ar rcs libvscpl1drv-logger.a $(CANALLOGGER_OBJECTS)

test:  test.o $(CANALLOGGER_OBJECTS)
	$(CXX) -o $@ test.o $(CANALLOGGER_OBJECTS) $(LDFLAGS) -L$(LIBDIRNAME) @LIBS@ $(EXTRALIBS)

vscpl1drv-logger.so: $(CANALLOGGER_OBJECTS)
	$(CXX) -o $@ $(CANALLOGGER_OBJECTS) $(DLFLAGS) -L$(LIBDIRNAME) @LIBS@ $(EXTRALIBS)

log.o: ../common/log.cpp ../common/log.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c ../common/log.cpp -o $@

guid.o: ../_vscp_common/guid.cpp ../_vscp_common/guid.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c ../_vscp_common/guid.cpp -o $@

dllist.o: ../_vscp_common_general/dllist.c ../_vscp_common_general/dllist.h
	$(CC) $(CFLAGS) -c ../_vscp_common_general/dllist.c -o $@

install: all
	$(STRIP) vscpl1drv-logger.so
	$(INSTALL_DIR) $(VSCP_PROJ_BASE_DIR)/drivers/level1/
	$(INSTALL_PROGRAM) vscpl1drv-logger..so $(VSCP_PROJ_BASE_DIR)/drivers/level1/

uninstall: all
	rm -rf $(VSCP_PROJ_BASE_DIR)/drivers/level1/vscpl1drv-logger.so

install-strip: install

man: ../man/vscpl1drv-logger.1.md
	pandoc -s -t man ../man/vscpl1drv-logger.1.md -o ../man/vscpl1drv-logger.1

deb:    all
	echo "\n" | ./builddist.sh
	@mkdir -p ../dist
	@mv *.deb ../dist

clean:
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../common/*.o
	rm -f ../_vscp_common_general/*.o
	rm -f ../_vscp_common/*.o
	rm -f vscpl1drv-logger.so
	rm -f test
	rm -f *.gz
	rm -f *.deb

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh Makefile

$(ALWAYS_BUILD):  .FORCE

.FORCE:

# Include dependency info, if present:
-include .deps/*.d

.PHONY: all install uninstall clean distclean data .FORCE
