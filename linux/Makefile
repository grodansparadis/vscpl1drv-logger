# =========================================================================
#                      Autogenerated from Makefile.in         
# =========================================================================

# STATIC = yes if a static build is requested.
STATIC=no
VSCP_PROJ_BASE_DIR=/srv/vscp

prefix = /usr/local
exec_prefix = ${prefix}
INSTALL = /usr/bin/install -c
top_srcdir = ../../../../../..
top_builddir = ../../../../../..

CC = gcc
CXX = g++

# get version from the version file
MAJOR_VERSION = `sed '35!d' ../../common/version.h  | cut -b 33-`
MINOR_VERSION = `sed '36!d' ../../common/version.h  | cut -b 33-`
RELEASE_VERSION = `sed '37!d' ../../common/version.h  | cut -b 33-`
BUILD_VERSION = `sed '38!d' ../../common/version.h  | cut -b 33-

CFLAGS =  -g -O0 -DOPENSSL_API_1_1 -fPIC -DCBC -I../../../../common -I../../../../../common
CXXFLAGS = -D__LINUX__ -fPIC -D_REENTRANT  -g -O0
CPPFLAGS = -D__LINUX__ -fPIC  -fno-var-tracking-assignments -I../../../../common -I../../../../../common

LDFLAGS = 
EXTRALIBS =  -lexpat -lssl -lz -lrt -lm -lcrypto -lpthread -ldl  

DLFLAGS = -g -shared -fPIC
STRIP = strip
INSTALL_PROGRAM = ${INSTALL}
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_DIR = /usr/bin/install -c -d
srcdir = .
top_srcdir = .
top_builddir =
bindir = ${exec_prefix}/bin
libdir = ${exec_prefix}/lib
datadir = ${prefix}/share
includedir = ${prefix}/include
DLLPREFIX = lib


### Variables: ###


CANALLOGGER_OBJECTS =  vscpl1drv-logger.o\
	log.o\
	dllist.o 

### Targets: ###

all: vscpl1drv-logger.so


test:  test.o $(PROJOBJS)
	$(CXX) -o $@ test.o $(PROJOBJS) $(LDFLAGS) -L$(LIBDIRNAME) $(EXTRALIBS)

vscpl1drv-logger.so: $(CANALLOGGER_OBJECTS)
	$(CXX) -o $@ $(CANALLOGGER_OBJECTS) $(DLFLAGS) -L$(LIBDIRNAME) $(EXTRALIBS) 

log.o: ../common/log.cpp ../common/log.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c ../common/log.cpp -o $@
	
guid.o: ../../../../common/guid.cpp ../../../../common/guid.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c ../../../../common/guid.cpp -o $@		

dllist.o: ../../../../../common/dllist.c ../../../../../common/dllist.h
	$(CC) $(CFLAGS) -c ../../../../../common/dllist.c -o $@

install: all
	mkdir -p $(VSCP_PROJ_BASE_DIR)/drivers/level1
	$(INSTALL_PROGRAM) vscpl1drv-logger.so $(VSCP_PROJ_BASE_DIR)/drivers/level1

uninstall: 

install-strip: install

deb:    all
	echo "\n" | ./builddist.sh
	@mkdir -p ../../../../../../dist
	@mv *.deb ../../../../../../dist

clean:
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../common/*.o
	rm -f ../../../../common/*.o
	rm -f ../../../../../common/*.o
	rm -f vscpl1drv-logger.so
	rm -f test
	rm -f *.gz
	rm -f *.deb

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh Makefile

$(ALWAYS_BUILD):  .FORCE

.FORCE: 

# Include dependency info, if present:
-include .deps/*.d

.PHONY: all install uninstall clean distclean data .FORCE



