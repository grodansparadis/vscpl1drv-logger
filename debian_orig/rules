#!/usr/bin/make -f

%:
	dh $@ --with autoreconf

override_dh_auto_install:
	strip linux/vscpl1drv-logger.so
	install -D -m 0755 linux/vscpl1drv-logger.so $$(pwd)/debian/vscpl1drv-logger/srv/vscp/drivers/level1/vscpl1drv-logger.so
